[database]
jdbcURL = "jdbc:sqlite:db/dinf.db"

[urls]

[urls.share]
salt = "salt"
length = 6

[urls.edit]
salt = "another_salt"
length = 24

[server]
port = 8080
baseURL = "http://localhost:8080"

[search]
url = "http://localhost:7700"
key = "key"
reindex = "60s"

[htmx]
timeout = "5s"
